# Intrusion-detection-system-
In this project , we built an intrusion detection system , where built the system based on two approaches , on with unsupervised learning and the other with supervised learning appraoch ,and then we deployed our models using joblib and fastapi , onto the elastic bean stalk , through pushing docker container onto the elastic repository.  
